# Legacy local-only example.
# Prefer `.picurv-execution.yml` when you want one site config to cover both
# login-node and batch runs.
#
# Leave this empty on ordinary local machines.
#
# Example override:
# local_execution:
#   launcher: "mpirun"
#   launcher_args:
#     - --bind-to
#     - none
local_execution: {}
