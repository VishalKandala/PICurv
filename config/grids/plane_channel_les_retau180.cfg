# ===================================================================
# Plane channel LES grid, Re_tau = 180 (Part B.4.2, coarse LES pass)
#
# Cartesian box (grid type `box` with flat walls, which is a plain
# Cartesian block) with two-sided tanh wall
# clustering in the wall-normal direction.
#
#   i (Xi)   : spanwise,     periodic          x in [0, 6.283185307179586]
#   j (Eta)  : wall-normal,  no-slip walls     y in [0, 2]   (half-height h = 1)
#   k (Zeta) : streamwise,   periodic, driven  z in [0, 12.566370614359172]
#
# WALL SPACING IS THE TUNING KNOB. `first_cell_j_start`/`_end` are given
# as a FRACTION OF THE AXIS LENGTH, not in physical units, so for the
# wall-normal axis of length Ly = 2:
#
#     first_cell_j = (y_plus_target / Re_tau) / Ly
#
# Shipped value targets y+_1 = 1.0 at Re_tau = 180: (1.0/180)/2 = 2.777778e-03.
# Resolution in wall units: dx+ (spanwise) = 34, dz+ (streamwise) = 68 -
# LES resolution, to be run with constant Smagorinsky only.
# Retarget y+ by editing these two numbers alone; nothing else in the
# case needs to change.
#
# `stretch_j` is only the initial guess for the tanh fit. The generator
# solves for the factor that actually realizes the requested first cell
# and reports it as "Computed stretch factor".
# ===================================================================

[box]
# --- Discretization ---
# Cell counts must stay ODD at every multigrid level: each level coarsens
# as IM -> (IM+1)/2. 33 -> 17 -> 9 -> 5 and 65 -> 33 -> 17 -> 9 support 4 levels
# (coarse grid 5x9x5 = 225).
ncells_i = 33
ncells_j = 65
ncells_k = 33

# --- Wall-normal clustering (two-sided: both walls) ---
stretch_j = 2.0
first_cell_j_start = 2.777778e-03
first_cell_j_end = 2.777778e-03

# --- Geometry ---
bounds_x = 0.0 6.283185307179586
bounds_y = 0.0 2.0
bounds_z = 0.0 12.566370614359172
origin = 0.0 0.0 0.0

# No wall shaping and no warp amplitudes: an exactly Cartesian block.

# --- Outputs ---
show_stats = yes
write_vtk = no
