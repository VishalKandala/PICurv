# Build Config Guide

This directory stores system-specific Make configuration fragments.

Files:
- `config.local.mk`
- `config.cluster.mk`
- `config.petsc.mk`

Used by `Makefile` via `SYSTEM=<name>`.
